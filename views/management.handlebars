   <!--sidebar start-->
    <div class="sidebar">
        <div class="sidebar-menu">
            <center class="Inventory">
                <h3>Overview</h3>
            </center>
                <a id="logout" href="index.html" class="logout">Logout</a>
        </div>

        <!--main container start-->
        <div class="main-container" id="mainDiv">
            
            <!-- View products -->
            <div class="filter-container">
                <label class="label">
                    <h3>View Products</h3>
                </label>

                <div class="select">
                    <select id="select">
                        <option>All Products</option>
    
                        <option id='catDairy'>Dairy</option>
                        <option id='catFruits'>Fruits</option>
                        <option id='catGrains'>Grains</option>
                        <option id='catProteins'>Proteins</option>
                        <option id='catVegetable'>Vegetables</option>
                        <option id='catOthers'>Others</option>

                        <!--
                        {{#each products}}
                        <option id="{{id}}">{{name}}</option>
                        
                        {{/each}}
                        -->
                    </select>
                </div>
            </div>

            <div class="container">
                <div id="productDiv" class="content">
                    <table id="productOverview">
                        <col style="width: 40%;"/>
                        <tr>
                            <th><label for='itemLabel'>Item</label></th>
                            <th><label for='qtyLabel'>QTY</label></th>
                            <th><label for='costLabel'>Cost</label></th>
                            <th><label for='categoryLabel'>Category</label></th>
                        </tr>
                        <tr id="productList">
                            <!--{{#each products}}-->
                            <td><label class="label" id="itemLabel">{{name}}</label></td>
                            <td><label class="label" id="qtyLabel">{{quantity}}</label></td>
                            <td><label class="label" id="costLabel">{{cost}}</label></td>
                            <td><label class="label" id="categoryLabel">{{category}}</label></td>
                            <!--{{/each}}-->
                        </tr>
                        <tr id="productList">
                            <!--{{#each products}}-->
                            <td><label class="label" id="itemLabel">{{name}}</label></td>
                            <td><label class="label" id="qtyLabel">{{quantity}}</label></td>
                            <td><label class="label" id="costLabel">{{cost}}</label></td>
                            <td><label class="label" id="categoryLabel">{{category}}</label></td>
                            <!--{{/each}}-->
                        </tr>
                    </table>
                            
                </div>
            </div>
        </div> <!--main container end-->
    </div> <!--sidebar end-->